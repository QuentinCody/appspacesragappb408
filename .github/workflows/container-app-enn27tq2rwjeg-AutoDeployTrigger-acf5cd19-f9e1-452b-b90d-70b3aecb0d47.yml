name: Trigger auto deployment for container-app-enn27tq2rwjeg

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-enn27tq2rwjeg-AutoDeployTrigger-acf5cd19-f9e1-452b-b90d-70b3aecb0d47.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPENN27TQ2RWJEG_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPENN27TQ2RWJEG_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPENN27TQ2RWJEG_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPENN27TQ2RWJEG_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPENN27TQ2RWJEG_REGISTRY_PASSWORD }}
          containerAppName: container-app-enn27tq2rwjeg
          resourceGroup: appspacesragappb4088b43
          imageToBuild: default/container-app-enn27tq2rwjeg:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


